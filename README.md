![Стек технологий](https://skillicons.dev/icons?i=github,kotlin,gradle,androidstudio,pytorch&theme=dark)


# 🚨 SafetyScooter — мобильное приложение для фиксации нарушений

**SafetyScooter** — Android-приложение, которое позволяет быстро зафиксировать нарушение, автоматически собрать важные данные (видео + геолокация) и отправить их на сервер для дальнейшей проверки.  
Проект создан как инструмент для повышения безопасности и документирования инцидентов.

---


[Видео крушения](https://github.com/user-attachments/assets/9873dc82-84ca-49cc-b5e6-87bed77975b4)


## Актуальность

С ростом темпа жизни и популярности электросамокатов количество нарушений на дорогах стремительно увеличивается, а инфраструктура и контроль не успевают за этим. 
Поэтому быстрые и надёжные инструменты фиксации инцидентов — такие как SafetyScooter — сейчас особенно востребованы.


---
## ✨ Ключевые возможности

### 🎥 1. Запись видеонарушений

- Максимальная длительность — **до 20 секунд**.
- Запись реализована через **CameraX**.
- Есть **таймер обратного отсчёта**.
- Возможность остановки записи в любой момент.
- Гарантированное завершение записи при уходе с экрана.

---

### 📍 2. Точный сбор GPS-координат

- Координаты собираются **одновременно с записью видео**.
- Минимум **одна GPS-точка на каждую секунду записи**.
- Используется **FusedLocationProviderClient** в режиме высокой точности.
- Геоданные отправляются на сервер вместе с видеозаписью.

---

### ☁️ 3. Отправка данных на сервер

- Видео и координаты отправляются **одним multipart-запросом**.
- HTTP-клиент — **OkHttp** + кастомный `RequestBody` для отслеживания прогресса.
- В интерфейсе отображается:
  - текущий прогресс передачи (сколько МБ отправлено / всего);
  - статус загрузки: _«идёт загрузка»_, _«успешно»_ или _«ошибка»_.
- Состояние загрузки реализовано через **StateFlow**, UI подписан на это состояние и обновляется в реальном времени.

---

### 🗂 4. История заявок

- Приложение получает список заявок с сервера.
- Для каждой заявки отображаются:
  - дата и время фиксации;
  - статус обработки (например, _«отправлено»_, _«проверяется»_, _«нарушение обнаружено»_, _«ошибка»_);
  - количество вердиктов по заявке;
  - локальный порядковый номер.
- Для отображения используется **RecyclerView** + **ListAdapter** + **DiffUtil**.

---

### 👤 5. Профиль пользователя

- Авторизация и регистрация по номеру телефона и паролю.
- Валидация номера телефона: формат `+7` и 10 цифр.
- Валидация пароля:
  - длина от 8 до 16 символов;
  - только латинские буквы и цифры.
- Сохранение `access_token` и номера телефона в **SharedPreferences**.
- Экран профиля:
  - показывает текущий номер пользователя;
  - содержит кнопку выхода, которая очищает сохранённые данные и возвращает пользователя к экрану входа.

---

## 🛠 Используемые технологии

- **Kotlin** — основной язык разработки.
- **CameraX** — работа с камерой и запись видео.
- **Kotlin Coroutines** — асинхронные операции.
- **StateFlow** — реактивное обновление интерфейса по состоянию загрузки и статусов.
- **ViewBinding** — безопасный доступ к элементам разметки.
- **RecyclerView + ListAdapter + DiffUtil** — эффективное отображение списков заявок.
- **FusedLocationProviderClient** — получение точных GPS-координат.
- **OkHttp** — HTTP-клиент.
- **Gson** — сериализация и десериализация JSON.
- **SharedPreferences** — хранение токена и минимальных пользовательских данных.

---

## 🔄 Логика работы приложения

1. Пользователь открывает приложение.
2. Если сохранён `access_token`, он сразу попадает на главный экран.  
   Если токена нет — открывается экран авторизации.
3. После входа пользователь может:
   - начать запись нарушения;
   - перейти к истории своих заявок;
   - открыть экран профиля.
4. При записи:
   - идёт съёмка видео;
   - параллельно собираются координаты.
5. После завершения записи пользователь попадает на экран просмотра видео и может отправить его на сервер.
6. Во время отправки отображается прогресс загрузки и результат (успех/ошибка).
7. Отправленные нарушения появляются в списке заявок.
8. При выходе из профиля токен и данные пользователя очищаются, и пользователь возвращается к экрану входа.

---

## 🎬 Демонстрация работы приложения

➡️ [Видео работы приложения](https://github.com/user-attachments/assets/a360330d-759e-4eb5-a102-0da694f804cd)

